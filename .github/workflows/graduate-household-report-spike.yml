name: Graduate Household Report Spike

on:
  push:
    branches:
      - refactor/graduate-household-report-spike

permissions:
  contents: write

jobs:
  refactor:
    if: github.event.head_commit.message == 'ci: graduate household report spike'
    runs-on: ubuntu-24.04
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Graduate stable Household report owners
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          import re

          old_consumption = Path('spike-src/HKernel/Spike/HouseholdConsumption.hs')
          old_report = Path('spike-src/HKernel/Spike/HouseholdReport.hs')
          old_render = Path('spike-src/HKernel/Spike/HouseholdReport/Render.hs')
          old_application = Path('spike-src/HKernel/Household/Application.hs')

          new_consumption = Path('household-src/HKernel/Household/BudgetObservation.hs')
          new_report = Path('household-src/HKernel/Household/Report.hs')
          new_render = Path('household-src/HKernel/Household/Report/Render.hs')
          new_application = Path('household-app-src/HKernel/Household/Application.hs')

          for destination in [new_consumption, new_report, new_render, new_application]:
              destination.parent.mkdir(parents=True, exist_ok=True)

          old_consumption.rename(new_consumption)
          old_report.rename(new_report)
          old_render.rename(new_render)
          old_application.rename(new_application)

          replacements = {
              'HKernel.Spike.HouseholdConsumption': 'HKernel.Household.BudgetObservation',
              'HKernel.Spike.HouseholdReport.Render': 'HKernel.Household.Report.Render',
              'HKernel.Spike.HouseholdReport': 'HKernel.Household.Report',
          }

          for path in list(Path('.').rglob('*.hs')) + list(Path('docs').rglob('*.md')) + [Path('README.md')]:
              if not path.exists():
                  continue
              text = path.read_text()
              updated = text
              for old, new in replacements.items():
                  updated = updated.replace(old, new)
              if updated != text:
                  path.write_text(updated)

          consumption = new_consumption.read_text()
          consumption = consumption.replace(
              '-- | Temporary adapter from admitted household Budget movements into one aligned\n-- domain observation.\n',
              '-- | Stable composition of admitted household Budget movements into one aligned\n-- domain observation.\n'
          )
          consumption = consumption.replace(
              '-- This\n-- adapter owns only the interpretation of ordered movements as named\n',
              '-- This\n-- owner interprets ordered movements as named\n'
          )
          new_consumption.write_text(consumption)

          report = new_report.read_text()
          report = report.replace(
              '-- | Read-only observation adapter for the current household source set.\n--\n-- General Budget policy, Household policy, and Daily Target meaning are owned\n-- by stable components. This module composes their typed values with retained\n-- compatibility sources; it does not write, migrate, or generate any source.\n',
              '-- | Pure Household report composition from already admitted typed values.\n--\n-- Source admission, writer authority, and delivery effects remain outside this\n-- module. It composes stable Household, Plan, Budget, Actual, and Report owners\n-- without reparsing physical compatibility sources.\n'
          )
          report = report.replace('  , buildHouseholdReportSurfaceFromPlanJournal\n', '')
          report = report.replace('import HKernel.Budget.Config (parseBudgetPolicy)\n', '')
          report = re.sub(
              r'import HKernel\.Household\.AccountProfile\.TSV\n  \(.*?\n  \)\n',
              '', report, flags=re.S
          )
          report = report.replace('import HKernel.Household.BudgetMovement.TSV\n', '')
          report = re.sub(
              r'import HKernel\.Household\.Config\n  \( parseHouseholdPolicy\n  \)\n',
              '', report
          )
          report = report.replace('import HKernel.Household.DailyTarget.TSV\n', '')
          report = report.replace('import HKernel.Household.Issue.TSV\n', '')
          report = report.replace('  , validateHouseholdPolicyAccounts\n', '')

          before = report
          report = re.sub(
              r'buildHouseholdReportSurfaceFromPlanJournal\n.*?(?=-- \| Calculate the Household report surface)',
              '', report, flags=re.S
          )
          if report == before:
              raise SystemExit('legacy Household report raw-source entry point was not removed')

          before = report
          report = re.sub(
              r'validatePlanJournalRegistry\n.*?(?=admitPlanJournal\n)',
              '', report, flags=re.S
          )
          if report == before:
              raise SystemExit('legacy Plan registry compatibility gate was not removed')

          before = report
          report = re.sub(
              r'budgetPolicySourceErrors ::.*?(?=resolveCycles\n)',
              '', report, flags=re.S
          )
          if report == before:
              raise SystemExit('legacy compatibility error translators were not removed')

          new_report.write_text(report)

          render = new_render.read_text()
          render = render.replace(
              '-- | Terminal publication of the read-only real household report observation adapter.\n--\n-- Note: This module is a temporary observation adapter renderer for private source data.\n-- It is not the canonical domain owner of Plan, cycle, budget, backing, allocation,\n-- lifecycle, tax, or filing semantics.\n',
              '-- | Pure terminal publication of an admitted Household report surface.\n--\n-- Rendering owns presentation only. Plan, cycle, Budget, backing, allocation,\n-- lifecycle, source admission, and writer semantics remain with their named owners.\n'
          )
          new_render.write_text(render)

          cabal_path = Path('h-kernel.cabal')
          cabal = cabal_path.read_text()
          household_marker = '                    , HKernel.Household.Policy\n'
          if household_marker not in cabal:
              raise SystemExit('household library module marker missing')
          cabal = cabal.replace(
              household_marker,
              household_marker
              + '                    , HKernel.Household.Report\n'
              + '                    , HKernel.Household.Report.Render\n'
              + '    other-modules:    HKernel.Household.BudgetObservation\n',
              1,
          )

          stable_application = '''library h-kernel-household-application
    exposed-modules:  HKernel.Household.Application
    build-depends:    base >= 4.14 && < 5
                    , containers >= 0.6 && < 0.8
                    , h-kernel
                    , h-kernel-household
                    , text >= 1.2 && < 2.2
                    , time >= 1.9 && < 1.15
    hs-source-dirs:   household-app-src
    default-language: Haskell2010
    ghc-options:      -Wall -Werror=incomplete-patterns
'''
          cabal, count = re.subn(
              r'library h-kernel-spike-household-report\n.*?(?=\nexecutable h-kernel\n)',
              stable_application.rstrip() + '\n',
              cabal,
              flags=re.S,
          )
          if count != 1:
              raise SystemExit(f'expected one spike library stanza, replaced {count}')
          cabal = cabal.replace('h-kernel-spike-household-report', 'h-kernel-household-application')

          executable_need = '''                    , h-kernel
                    , h-kernel-household-application
                    , text >= 1.2 && < 2.2
'''
          executable_with_household = '''                    , h-kernel
                    , h-kernel-household
                    , h-kernel-household-application
                    , text >= 1.2 && < 2.2
'''
          if executable_need not in cabal:
              raise SystemExit('h-kernel executable dependency marker missing')
          cabal = cabal.replace(executable_need, executable_with_household, 1)
          cabal_path.write_text(cabal)

          ownership_old = Path('docs/HOUSEHOLD_REPORT_SPIKE_REVIEW.md')
          ownership_new = Path('docs/HOUSEHOLD_REPORT_OWNERSHIP.md')
          if ownership_old.exists():
              ownership_old.rename(ownership_new)
          ownership_new.write_text('''# Household Report ownership

ステータス: アクティブなstable boundary  
更新日: 2026-08-11

## 目的

Household Report の現在のownerと依存方向を記録する。旧 `Spike` placement は、canonical Household admission がstable ownerへ移る前の暫定境界だった。現在のproduction pathはその条件を満たしたため、Report compositionをstable Household componentへ卒業させる。

## 現在の構成

```text
canonical Household sources
  -> HKernel.Household.Application
       -> typed HouseholdState
       -> HKernel.Household.Report
            -> HKernel.Household.BudgetObservation
            -> stable Plan / Actual / Budget / Backing / DailyTarget owners
       -> HKernel.Household.Report.Render
  -> CLI / TUI delivery
```

### `HKernel.Household.Report`

already admittedなtyped valueだけを受け取り、次を一つのread-only `HouseholdReportSurface`へ合成する。

- current / previous cycle observation
- open Plan とcompletion evidence
- Budget observation
- Household Backing
- Daily Target
- Household Issues

物理source parser、filesystem IO、writer authorityを所有しない。

### `HKernel.Household.BudgetObservation`

ordered `HouseholdBudgetMovement` とvalidated Household policyを、Consumption / Entitlement / Remainingへ揃えるpure ownerである。旧 `HKernel.Spike.HouseholdConsumption` の責任をそのままstable Household vocabularyへ移したもので、generic accounting frameworkは導入しない。

### `HKernel.Household.Report.Render`

already typedなReport surfaceのpresentationだけを所有する。stable `HKernel.Render` / `HKernel.Render.TerminalStyle`を再利用し、accounting calculationやsource admissionを再実装しない。

### `HKernel.Household.Application`

canonical 8-source rootのIO bootstrap、typed admission、`HouseholdState`、write snapshotを所有する。pure Household libraryへIOを混ぜないため、Cabal component `h-kernel-household-application` に分離する。

## 退役した暫定境界

旧 `buildHouseholdReportSurfaceFromPlanJournal` は、`accounts.tsv`、`budget_alloc.tsv`、`daily_target_scope.tsv` 等のretained compatibility sourceをReport moduleから直接admitしていた。current production deliveryはすでにこの入口を使用せず、canonical `HouseholdState`からtyped report calculationへ進む。

そのため、Report compositionから旧raw compatibility入口とsource-local parser wiringを削除する。retained compatibility parser自体の存在や、別repositoryのreader/writer authorityをこの決定から変更しない。

## 維持する境界

- exact arithmeticを維持する
- Account / Plan / Actual identityを推測しない
- provenanceとcompletion relationをtyped evidenceから扱う
- source admissionとReport calculationを混ぜない
- rendererへdomain calculationを移さない
- application IOをpure Household libraryへ混ぜない
- private canonical sourceをpublic testやdiagnosticへ持ち込まない
- writer authorityはReportのmodule placementから推測しない

## 教材としての意味

`Spike`という名前は「まだ意味が確定していない実験」を示す。production CLI/TUIが恒常的に依存し、canonical typed admissionからReportを生成する現在の構造を`Spike`と呼び続けると、実際のownershipを誤って教える。

今回の卒業は抽象化の追加ではなく、すでに成立しているownerへ名前とcomponent配置を一致させる整理である。
''')

          for path in list(Path('docs').rglob('*.md')) + [Path('README.md'), Path('docs/INDEX.toml')]:
              if not path.exists():
                  continue
              text = path.read_text()
              updated = text.replace('HOUSEHOLD_REPORT_SPIKE_REVIEW.md', 'HOUSEHOLD_REPORT_OWNERSHIP.md')
              updated = updated.replace('h-kernel-spike-household-report', 'h-kernel-household-application')
              if updated != text:
                  path.write_text(updated)

          test = r'''{-# LANGUAGE OverloadedStrings #-}

module Main (main) where

import qualified Data.List.NonEmpty as NonEmpty
import Data.Text (Text)
import qualified Data.Text as T
import Data.Time.Calendar (fromGregorian)
import HKernel.Application.Config (mkHouseholdRoot)
import HKernel.Household.Application
  ( admitCanonicalHousehold
  , buildHouseholdReportSurfaceFromHousehold
  )
import HKernel.Household.Report
import HKernel.Household.Report.Render
import HKernel.HouseholdIssue
import HKernel.Money
import HKernel.Period
import HKernel.Plan
import HKernel.Report.CycleAccounts
import HKernel.Report.Presentation (defaultPresentationConfig)
import System.Exit (exitFailure)

main :: IO ()
main = do
  let observation = fromGregorian 2026 7 31
      surface = mustRightText
        (buildSurfaceAt observation actualJournal planJournalText householdTOML issuesTSV)
      currentCycle = householdCurrentCycleAccounts surface
      currentPeriod = currentCycleAccountsPeriod currentCycle
      backing = householdEnvelopeBacking surface
      target = householdDailyTarget surface
      foodLine = exactlyOne (envelopeBackingLines backing)
      openPlans = householdPlannedTransactions surface
      classifiedPlans = classifyPlannedTransactions currentPeriod openPlans
      issue = exactlyOne
        (filter ((== Open) . householdIssueStatus) (householdIssues surface))
      jpy = mustRight (mkCommodity "JPY")

  assertEqual "current income-anchor cycle is resolved from Actual and Plan Journal"
    (fromGregorian 2026 6 15, fromGregorian 2026 8 14)
    (periodStart currentPeriod, periodEndExclusive currentPeriod)
  assertEqual "precise current-cycle report observes through the Household observation day"
    observation
    (currentCycleAccountsObservation currentCycle)
  assertEqual "precise current-cycle report publishes the complete declared Account axis"
    8
    (length (currentCycleAccountsRows currentCycle))

  case householdCycleComparison surface of
    HouseholdCycleComparisonAvailable comparison -> do
      let baseline = cycleComparisonBaseline comparison
      assertEqual "previous cycle is resolved from the two latest Actual anchors"
        (fromGregorian 2026 4 15, fromGregorian 2026 6 15)
        ( periodStart (currentCycleAccountsPeriod baseline)
        , periodEndExclusive (currentCycleAccountsPeriod baseline)
        )
      assertEqual "Household daily comparison uses aligned elapsed policy"
        AlignedElapsed
        (cycleComparisonPolicy comparison)
      assertEqual "Household daily comparison observes the previous cycle at the same elapsed day"
        (fromGregorian 2026 5 31)
        (currentCycleAccountsObservation baseline)
    unavailable -> failWith "aligned Household Cycle Comparison should be available" unavailable

  assertEqual "explicit completion removes the completed outgoing Plan without hiding other horizons"
    ["plan-prior", "plan-overdue", "plan-wifi", "plan-next-cycle"]
    (map (planIdText . committedPlanId) openPlans)
  assertEqual "open outgoing Plans remain date ordered across the current cycle boundary"
    [ fromGregorian 2026 6 10
    , fromGregorian 2026 7 25
    , fromGregorian 2026 8 8
    , fromGregorian 2026 8 20
    ]
    (map committedPlanDate openPlans)
  assertEqual "Plan horizon classification keeps before current and after current visible"
    [BeforeCurrentCycle, InCurrentCycle, InCurrentCycle, AfterCurrentCycle]
    (map classifiedPlanHorizon classifiedPlans)
  assertEqual "issue category evidence remains visible without affecting balances"
    "[planning] decide funding"
    (householdIssueDetails issue)
  assertEqual "native Budget movement becomes exact envelope entitlement"
    (one jpy 1000)
    (envelopeEntitlement foodLine)
  assertEqual "Actual Expense movement becomes exact consumption"
    (one jpy 100)
    (envelopeActualConsumption foodLine)
  assertEqual "only current-cycle open mapped Plans become reserve and derived headroom"
    (one jpy 600)
    (envelopePostPlanHeadroom foodLine)
  assertEqual "liquid Asset backing is selected by explicit Budget policy"
    (one jpy 1900)
    (envelopeFundingBalance backing)
  assertEqual "Daily Target Asset selection is owned by household.toml"
    (one jpy 1900)
    (dailyTargetEligibleAssets target)
  assertEqual "Daily Target retains native Plan reservation evidence"
    (one jpy 50)
    (dailyTargetAlreadyExcluded target)
  assertEqual "Daily Target keeps capacity net of bounded reservation evidence"
    (one jpy 1750)
    (dailyTargetCapacity target)
  assertEqual "Daily Target publishes an exact rational rate"
    [(jpy, 125)]
    (dailyTargetRate target)

  let renderedSurface = renderHouseholdReportSections
        defaultPresentationConfig surface
  assertEqual "Household cycle delivery publishes the precise current-cycle report"
    True
    ("Current Cycle Accounts" `T.isInfixOf` renderedSurface
      && "Opening" `T.isInfixOf` renderedSurface
      && "Closing" `T.isInfixOf` renderedSurface)
  assertEqual "Household cycle delivery publishes the aligned previous-cycle comparison"
    True
    ("Cycle Comparison" `T.isInfixOf` renderedSurface
      && "Policy: aligned_elapsed" `T.isInfixOf` renderedSurface
      && "2026-05-31" `T.isInfixOf` renderedSurface)
  assertEqual "legacy Expense-only matrix no longer owns visible Household cycle output"
    False
    ("Cycle Accounts & Comparison Matrix" `T.isInfixOf` renderedSurface)
  assertEqual "Planned Transactions identifies the native Plan Journal source"
    True
    ("Source: plan.journal" `T.isInfixOf` renderedSurface)
  assertEqual "Planned Transactions exposes still-open commitments before the current cycle"
    True
    ("Open before current cycle" `T.isInfixOf` renderedSurface
      && "2026-06-10 | plan-prior" `T.isInfixOf` renderedSurface)
  assertEqual "Planned Transactions marks the resolved current-cycle boundary"
    True
    ("current cycle ends before 2026-08-14" `T.isInfixOf` renderedSurface)
  assertEqual "Planned Transactions keeps the next-cycle payment visible before cycle rollover"
    True
    ("2026-08-20 | plan-next-cycle" `T.isInfixOf` renderedSurface)

  let shortPreviousActual =
        T.replace "2026-04-15 Pension" "2026-05-15 Pension" actualJournal
      shortPreviousSurface = mustRightText
        (buildSurfaceAt observation shortPreviousActual planJournalText householdTOML issuesTSV)
      shortPreviousRendered = renderHouseholdReportSections
        defaultPresentationConfig shortPreviousSurface
  case householdCycleComparison shortPreviousSurface of
    HouseholdCycleComparisonUnavailable _ ->
      assertEqual "unavailable aligned comparison does not fail the Household surface"
        True
        ("Status: NOT AVAILABLE" `T.isInfixOf` shortPreviousRendered
          && "Daily Target" `T.isInfixOf` shortPreviousRendered
          && "Envelope & Backing" `T.isInfixOf` shortPreviousRendered)
    available -> failWith "short previous cycle should make aligned comparison unavailable" available

  let defaultIssues = renderHouseholdIssues OpenIssuesOnly (householdIssues surface)
      allIssues = renderHouseholdIssues AllIssues (householdIssues surface)
  assertEqual "default Issue report uses vertical cards"
    True
    ("┌─ OPEN " `T.isInfixOf` defaultIssues
      && "│ Title" `T.isInfixOf` defaultIssues
      && not ("Issue ID" `T.isInfixOf` defaultIssues))
  assertEqual "resolved Issues are hidden by default"
    False
    ("issue-resolved" `T.isInfixOf` defaultIssues)
  assertEqual "the explicit all-Issues option retains resolved cards"
    True
    ("issue-resolved" `T.isInfixOf` allIssues
      && "┌─ RESOLVED " `T.isInfixOf` allIssues)

  assertLeftContaining "Household cycle Account must be declared Income"
    "HouseholdCycleIncomeAccountNotIncome"
    (buildSurfaceAt observation actualJournal planJournalText cycleRoleMismatchTOML issuesTSV)
  assertLeftContaining "Household allocation Account must be declared Budget"
    "HouseholdAllocationAccountNotBudget"
    (buildSurfaceAt observation actualJournal planJournalText allocationRoleMismatchTOML issuesTSV)
  assertLeftContaining "unsupported Plan Journal directions fail at admission"
    "UnsupportedPlanRoleFlow"
    (buildSurfaceAt observation actualJournal unsupportedPlanJournalText householdTOML issuesTSV)
  assertLeftContaining "unknown completion Plan references fail closed"
    "unknown PlanId"
    (buildSurfaceAt observation unknownPlanActualJournal planJournalText householdTOML issuesTSV)
  assertLeftContaining "Daily Target rejects reservation above the Plan amount"
    "ReservationExceedsPlanAmount"
    (buildSurfaceAt observation actualJournal overReservedPlanJournalText householdTOML issuesTSV)
  assertLeftContaining "Daily Target rejects cross-Commodity reservation"
    "ReservationCommodityMismatch"
    (buildSurfaceAt observation actualJournal crossCommodityPlanJournalText householdTOML issuesTSV)
  assertLeftContaining "Daily Target rejects duplicate reservation identity"
    "DuplicateReservationId"
    (buildSurfaceAt observation actualJournal duplicateReservationPlanJournalText householdTOML issuesTSV)

one :: Commodity -> Integer -> Balance
one commodity value = singletonBalance
  (mkAmount commodity (quantityFromInteger value))

buildSurfaceAt
  :: Data.Time.Calendar.Day
  -> Text
  -> Text
  -> Text
  -> Text
  -> Either Text HouseholdReportSurface
buildSurfaceAt observation actual plan household issues = do
  state <- leftShow
    (admitCanonicalHousehold householdRoot accountsJournal actual plan budgetJournal
      budgetPolicyTOML household reportTOML issues)
  leftShow (buildHouseholdReportSurfaceFromHousehold observation state)

householdRoot = case mkHouseholdRoot "." of
  Right value -> value
  Left err -> error ("invalid Household root fixture: " ++ show err)

leftShow :: Show error => Either error value -> Either Text value
leftShow result = case result of
  Left err -> Left (T.pack (show err))
  Right value -> Right value

accountsJournal :: Text
accountsJournal = declarations

actualJournal :: Text
actualJournal = "include accounts.journal\n\n" <> T.unlines
  [ "2026-04-15 Pension"
  , "    assets:cash  1000 JPY"
  , "    income:pension"
  , ""
  , "2026-06-15 Pension"
  , "    assets:cash  1000 JPY"
  , "    income:pension"
  , ""
  , "2026-07-20 Food"
  , "    expenses:food  100 JPY"
  , "    assets:cash"
  , ""
  , "2026-07-25 Card settlement"
  , "    ; event-id: actual-card"
  , "    ; plan-id: plan-card"
  , "    liabilities:card  400 JPY"
  , "    assets:savings"
  ]

unknownPlanActualJournal :: Text
unknownPlanActualJournal = actualJournal <> T.unlines
  [ ""
  , "2026-07-26 Unknown completion"
  , "    ; event-id: actual-unknown"
  , "    ; plan-id: plan-missing"
  , "    liabilities:card  1 JPY"
  , "    assets:savings"
  ]

declarations :: Text
declarations = T.unlines
  [ "account assets:cash"
  , "    type: asset"
  , "    commodity: JPY"
  , ""
  , "account assets:savings"
  , "    type: asset"
  , "    commodity: JPY"
  , ""
  , "account liabilities:card"
  , "    type: liability"
  , "    commodity: JPY"
  , ""
  , "account income:pension"
  , "    type: income"
  , "    commodity: JPY"
  , ""
  , "account expenses:food"
  , "    type: expense"
  , "    commodity: JPY"
  , ""
  , "account budget:opening"
  , "    type: budget"
  , "    commodity: JPY"
  , ""
  , "account budget:unassigned"
  , "    type: budget"
  , "    commodity: JPY"
  , ""
  , "account budget:food"
  , "    type: budget"
  , "    commodity: JPY"
  ]

budgetJournal :: Text
budgetJournal = "include accounts.journal\n\n" <> T.unlines
  [ "2026-06-15 allocate"
  , "    budget:opening  -1000 JPY"
  , "    budget:food      1000 JPY"
  , ""
  , "2026-06-15 unassigned"
  , "    budget:opening     -50 JPY"
  , "    budget:unassigned   50 JPY"
  ]

budgetPolicyTOML :: Text
budgetPolicyTOML = T.unlines
  [ "[[backing-pools]]"
  , "id = \"operating\""
  , "asset-accounts = [\"assets:cash\"]"
  , ""
  , "[[envelopes]]"
  , "id = \"Food\""
  , "label = \"Food\""
  , "pacing = \"daily\""
  , "backing-pool = \"operating\""
  , "expense-accounts = [\"expenses:food\"]"
  ]

householdTOML :: Text
householdTOML = T.unlines
  [ "[cycle]"
  , "mode = \"income-anchor\""
  , "income-account = \"income:pension\""
  , ""
  , "[budget]"
  , "unassigned-accounts = [\"budget:unassigned\"]"
  , ""
  , "[[budget.envelopes]]"
  , "id = \"Food\""
  , "allocation-account = \"budget:food\""
  , ""
  , "[daily-target]"
  , ""
  , "[[daily-target.assets]]"
  , "id = \"cash\""
  , "account = \"assets:cash\""
  ]

cycleRoleMismatchTOML :: Text
cycleRoleMismatchTOML = T.replace
  "income-account = \"income:pension\""
  "income-account = \"assets:cash\""
  householdTOML

allocationRoleMismatchTOML :: Text
allocationRoleMismatchTOML = T.replace
  "allocation-account = \"budget:food\""
  "allocation-account = \"expenses:food\""
  householdTOML

planJournalText :: Text
planJournalText = "include accounts.journal\n\n" <> T.unlines
  [ "2026-06-10 prior cycle"
  , "    ; plan-id: plan-prior"
  , "    expenses:food    50 JPY"
  , "    assets:cash     -50 JPY"
  , ""
  , "2026-07-25 overdue"
  , "    ; plan-id: plan-overdue"
  , "    expenses:food   100 JPY"
  , "    assets:cash    -100 JPY"
  , ""
  , "2026-08-08 wifi"
  , "    ; plan-id: plan-wifi"
  , "    ; daily-target-id: wifi"
  , "    ; reservation-id: reservation:wifi"
  , "    ; reservation-amount: 50"
  , "    ; reservation-commodity: JPY"
  , "    expenses:food   200 JPY"
  , "    assets:cash    -200 JPY"
  , ""
  , "2026-08-10 card"
  , "    ; plan-id: plan-card"
  , "    liabilities:card   400 JPY"
  , "    assets:cash       -400 JPY"
  , ""
  , "2026-08-14 pension"
  , "    ; plan-id: plan-pension"
  , "    income:pension  -1000 JPY"
  , "    assets:cash      1000 JPY"
  , ""
  , "2026-08-20 next cycle"
  , "    ; plan-id: plan-next-cycle"
  , "    expenses:food   300 JPY"
  , "    assets:cash    -300 JPY"
  ]

unsupportedPlanJournalText :: Text
unsupportedPlanJournalText = planJournalText <> T.unlines
  [ ""
  , "2026-08-10 transfer"
  , "    ; plan-id: plan-transfer"
  , "    assets:savings   10 JPY"
  , "    assets:cash     -10 JPY"
  ]

overReservedPlanJournalText :: Text
overReservedPlanJournalText = T.replace
  "; reservation-amount: 50"
  "; reservation-amount: 300"
  planJournalText

crossCommodityPlanJournalText :: Text
crossCommodityPlanJournalText = T.replace
  "; reservation-commodity: JPY"
  "; reservation-commodity: USD"
  planJournalText

duplicateReservationPlanJournalText :: Text
duplicateReservationPlanJournalText = T.replace
  "; reservation-id: reservation:wifi"
  "; reservation-id: reservation:shared"
  (T.replace
    "    ; plan-id: plan-overdue\n"
    (T.unlines
      [ "    ; plan-id: plan-overdue"
      , "    ; daily-target-id: overdue"
      , "    ; reservation-id: reservation:shared"
      , "    ; reservation-amount: 25"
      , "    ; reservation-commodity: JPY"
      ])
    planJournalText)

issuesTSV :: Text
issuesTSV = T.unlines
  [ "issue_id\tstatus\tdate\tcategory\ttitle\tamount\tcurrency\tdetails"
  , "issue-one\topen\t2026-07-20\tplanning\twifi\t200\tJPY\tdecide funding"
  , "issue-resolved\tresolved\t2026-07-01\tsubscription\tcancelled\t0\tJPY\tdone"
  ]

reportTOML :: Text
reportTOML = T.unlines
  [ "[presentation.amounts]"
  , "negative-style = \"parentheses\""
  , ""
  , "[reports.trial-balance]"
  , "as-of = \"latest\""
  , ""
  , "[reports.balance-sheet]"
  , "as-of = \"latest\""
  , ""
  , "[reports.profit-and-loss]"
  , "from = \"2026-07-01\""
  , "through = \"latest\""
  , ""
  , "[reports.daily-flow]"
  , "from = \"2026-07-01\""
  , "through = \"latest\""
  , "max-date-columns = 5"
  , ""
  , "[reports.monthly-accounts]"
  , "from = \"2026-07-01\""
  , "through = \"latest\""
  , ""
  , "[reports.recent-transactions]"
  , "through = \"latest\""
  , "count = 5"
  ]

exactlyOne :: Show value => [value] -> value
exactlyOne [value] = value
exactlyOne values = error ("expected exactly one value, got " ++ show values)

mustRight :: Show error => Either error value -> value
mustRight (Right value) = value
mustRight (Left err) = error ("invalid test fixture: " ++ show err)

mustRightText :: Either Text value -> value
mustRightText (Right value) = value
mustRightText (Left err) = error ("invalid test fixture: " ++ T.unpack err)

assertLeftContaining :: String -> Text -> Either Text value -> IO ()
assertLeftContaining label expected result = case result of
  Left err
    | expected `T.isInfixOf` err -> putStrLn ("  [PASS] " ++ label)
    | otherwise -> do
        putStrLn ("  [FAIL] " ++ label)
        putStrLn ("    error did not contain: " ++ T.unpack expected)
        putStrLn ("    actual error: " ++ T.unpack err)
        exitFailure
  Right _ -> do
    putStrLn ("  [FAIL] " ++ label)
    putStrLn "    unexpectedly accepted source"
    exitFailure

assertEqual :: (Eq value, Show value) => String -> value -> value -> IO ()
assertEqual label expected actual
  | expected == actual = putStrLn ("  [PASS] " ++ label)
  | otherwise = do
      putStrLn ("  [FAIL] " ++ label)
      putStrLn ("    expected: " ++ show expected)
      putStrLn ("    but got:  " ++ show actual)
      exitFailure

failWith :: Show value => String -> value -> IO a
failWith label actual = do
  putStrLn ("  [FAIL] " ++ label)
  putStrLn ("    actual: " ++ show actual)
  exitFailure
'''
          # Avoid a qualified Day type solely for one signature.
          test = test.replace('  :: Data.Time.Calendar.Day\n', '  :: Day\n')
          test = test.replace(
              'import Data.Time.Calendar (fromGregorian)\n',
              'import Data.Time.Calendar (Day, fromGregorian)\n'
          )
          Path('tests/HouseholdReportSpec.hs').write_text(test)

          # Keep current architecture names honest without rewriting historical Git data.
          for path in [Path('README.md'), Path('docs/ARCHITECTURE.md'), Path('docs/CODE_MAP_AND_DESIGN_SKETCH.md'), Path('docs/CURRENT_STATE_AUDIT_2026-08-09.md'), Path('docs/ARCHITECTURE_CODE_QUALITY_REVIEW.md')]:
              if path.exists():
                  text = path.read_text()
                  text = text.replace('spike-src/HKernel/Household/Application.hs', 'household-app-src/HKernel/Household/Application.hs')
                  text = text.replace('spike-src/HKernel/Spike/HouseholdReport.hs', 'household-src/HKernel/Household/Report.hs')
                  text = text.replace('spike-src/HKernel/Spike/HouseholdReport/Render.hs', 'household-src/HKernel/Household/Report/Render.hs')
                  text = text.replace('spike-src/HKernel/Spike/HouseholdConsumption.hs', 'household-src/HKernel/Household/BudgetObservation.hs')
                  path.write_text(text)

          # The branch should contain no production Spike namespace or component.
          old_module_hits = []
          for path in list(Path('.').rglob('*.hs')) + [Path('h-kernel.cabal')]:
              if '.git' in path.parts:
                  continue
              text = path.read_text(errors='replace')
              if 'HKernel.Spike.' in text or 'h-kernel-spike-household-report' in text:
                  old_module_hits.append(str(path))
          if old_module_hits:
              raise SystemExit('stale production Spike references: ' + ', '.join(old_module_hits))
          PY

          rm .github/workflows/graduate-household-report-spike.yml
          rmdir -p spike-src/HKernel/Spike/HouseholdReport 2>/dev/null || true
          rmdir -p spike-src/HKernel/Spike 2>/dev/null || true
          rmdir -p spike-src/HKernel 2>/dev/null || true
          rmdir spike-src 2>/dev/null || true

          echo '=== final changed files ==='
          git status --short
          echo '=== stale spike references (docs may retain historical prose) ==='
          grep -R -n -E 'HKernel\.Spike\.|h-kernel-spike-household-report|spike-src/' README.md docs h-kernel.cabal app editor-app editor-tui-app household-src household-app-src tests 2>/dev/null || true

      - name: Commit refactor
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'shumoku88-bit'
          git config user.email 'shumoku88@gmail.com'
          git add -A
          git commit -m 'refactor(household): graduate report spike'
          git push origin HEAD:refactor/graduate-household-report-spike
